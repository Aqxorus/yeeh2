export default interface IConfig {
  token: string;
  token1: string;
  apiKey: string;
  heartbeat: string;
  baseURL: string;
  mongoUri: string;
  defaultCooldown: number;
  debugMode: number;
  discordClientId: string;
  guildId: string;
}
